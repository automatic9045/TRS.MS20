<UserControl x:Class="TRS.MS20.Presentation.Components.MenuBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:docs="clr-namespace:System.Windows.Documents;assembly=PresentationFramework"
             xmlns:local="clr-namespace:TRS.MS20.Presentation.Components"
             mc:Ignorable="d"
             Background="White"
             d:DesignHeight="20" d:DesignWidth="1024">
    <UserControl.Resources>
        <Style TargetType="Button">
            <Style.Setters>
                <Setter Property="OverridesDefaultStyle" Value="True"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Label Padding="0" FontWeight="Bold" Content="{TemplateBinding Content}"/>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style.Setters>
        </Style>
    </UserControl.Resources>
    <StackPanel Margin="10 0" Orientation="Horizontal">
        <Button Content="端末種別(_T)"/>
    </StackPanel>
</UserControl>
